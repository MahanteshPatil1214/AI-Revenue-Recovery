# Evolution API gateway auth key (used for admin + instance operations).
# Set to a long random secret; mirror the same value in backend/revenueRecovery/.env -> EVOLUTION_API_KEY.
EVOLUTION_AUTH_KEY=change-me-to-a-long-random-secret
